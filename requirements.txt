# Core Computer Vision & AI
opencv-python==4.10.0.84      # Face detection and image processing (PRIMARY, compatible with numpy 1.26.4)
protobuf>=4.25.3,<5.0         # Protocol buffers (required by MediaPipe 0.10.21, compatible with TensorFlow 2.15.0)
mediapipe>=0.10.21            # Liveness detection and face landmarks (FALLBACK)
ultralytics>=8.0.0            # YOLO face detection for class attendance
deepface==0.0.95              # Face recognition and embedding extraction (ArcFace model - fully supported)
tensorflow==2.15.0            # Deep learning backend for DeepFace (compatible with ArcFace in DeepFace 0.0.95)
tf-keras==2.15.0              # Keras for TensorFlow 2.15.0 (pinned to avoid compatibility issues with ArcFace)
numpy==1.26.4                 # Numerical computing (compatible with MediaPipe <2.0 requirement)
pandas==2.3.2                 # Data manipulation and CSV handling
openpyxl==3.1.5               # Excel file support for data export
statsmodels==0.14.5           # Statistical modeling and trend analysis

# Web Interface & Dashboard
streamlit==1.48.1             # Modern web dashboard
plotly==6.3.0                 # Interactive charts and analytics

# Additional Face Detection Models
mtcnn==1.0.0                  # Alternative face detection
retina-face==0.0.17           # High-accuracy face detection

# Web Framework & Deployment
flask==3.1.2                  # Web API framework (legacy)
flask-cors==6.0.1             # Cross-origin resource sharing (legacy)
gunicorn==23.0.0              # WSGI server for deployment (legacy)
fastapi==0.115.0              # Modern REST API framework
uvicorn[standard]==0.32.0     # ASGI server for FastAPI
python-multipart==0.0.9      # For file uploads in FastAPI
pillow==11.0.0                # Image processing (PIL)

# Testing
pytest==8.4.1                 # Testing framework
